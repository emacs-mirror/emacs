#!/usr/local/bin/perl -w
# $HopeName: MMQA_harness!script:commands:clib(trunk.6) $
#
# Compile all the .c files in $testlib_dir,
# putting the resulting .o files in $obj_dir.
#

&requiredoptions(
 "MPS_INCLUDE_DIR",
 "MPS_INTERFACE_VERSION",
 "MPS_PRODUCT",
 "PLATFORM"
);

&clib;

